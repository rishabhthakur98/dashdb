services:
  dashdb:
    build:
      context: .
      dockerfile: Dockerfile
    image: dashdb:0.1.0-prod
    ports:
      - "8080:8080"
    environment:
      - SERVER_URL=0.0.0.0:8080
      - LOG_LEVEL=info
      - AUTH_TOKEN=TOKEN
    restart: unless-stopped